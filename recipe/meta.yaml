{% set name = "dnsruby" %}
{% set version = "1.61.6" %}

package:
  name: rb-{{ name|lower }}
  version: {{ version }}

source:
  url: https://rubygems.org/downloads/{{ name }}-{{ version }}.gem
  sha256: a37e3300f252d249deb7a73d933d689895a9a259f88dcee0e81d0a38a130716f

build:
  noarch: generic
  number: 0
  script:
    - gem install -N -l -V --norc --ignore-dependencies {{ name }}-{{ version }}.gem
    - gem unpack {{ name }}-{{ version }}.gem
  skip: {{ win }}

requirements:
  host:
    - ruby
  run:
    - ruby
    - rb-addressable >=2.5,<3

test:
  commands:
    - ruby -r {{ name }} -e 'exit 0'

about:
  home: https://rubygems.org/gems/{{ name }}
  license: Apache-2.0
  license_family: Apache
  license_file: {{ name }}-{{ version }}/LICENSE
  summary: Dnsruby is a pure Ruby DNS client library which implements a stub resolver. It aims to comply with all DNS RFCs, including DNSSEC NSEC3 support.

  doc_url: https://www.rubydoc.info/gems/{{ name }}
  dev_url: https://github.com/alexdalitz/{{ name }}

extra:
  recipe-maintainers:
    - sodre
